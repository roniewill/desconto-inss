<div class="row justify-content-center">

   <div class="col-5">

     <div class="mt-3">

       <h1> Cadastrar </h1>

     </div>

     <%= render "shared/error_messages", resource: resource %>

     <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |form| %>

        <div class="form-group row mt-3">

          <%= form.label :first_name, "Nome" %>

          <%= form.text_field :first_name, autofocus: true, class: "form-control", placeholder: "Digite seu primeiro nome" %>

        </div>

        <div class="form-group row mt-3">

          <%= form.label :last_name, "Sobrenome" %>

          <%= form.text_field :last_name, autofocus: true, class: "form-control", placeholder: "Digite seu sobrenome" %>

        </div>

        <div class="form-group row mt-3">

          <%= form.label :email, "Email" %>

          <%= form.text_field :email, autofocus: true, class: "form-control", placeholder: "Digite seu endereço de e-mail" %>

        </div>

        <div class="form-group row mt-3">

          <%= form.label :password, "Senha"%>

          <%= form.password_field :password, autofocus: true, class: "form-control", placeholder: "Digite a senha" %>

        </div>

        <div class="form-group row mt-3">

          <%= form.label :password_confirmation, "Confirme a Senha" %>

          <%= form.password_field :password_confirmation, autofocus: true, class: "form-control", placeholder: "Confirme a senha" %>

        </div>

        <div class=" form-group row mt-3">

         <%= form.submit "Cadastrar", class: "btn btn-primary" %>

        </div>

     <% end %>

     <%= render "users/shared/links" %>

   </div>

 </div>
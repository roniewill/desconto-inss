<div class="col-5 mt-4 bg-light border rounded-3">

    <div class="mt-3">

        <h2>Detalhes do Funcionário #<%= employee.id %></h2>

    </div>

    <p class="lead">Nome: <b class="font-monospace"><%= employee.first_name %></b></p>
    <p class="lead">Sobrenome: <b class="font-monospace"><%= employee.last_name %></b></p>
    <p class="lead">CPF: <b class="font-monospace"><%= employee.cpf %></b></p>
    <p class="lead">Data de nascimento: <b class="font-monospace"><%= employee.birth_date.strftime('%d/%m/%Y') %></b></p>
    <p class="lead">Celular: <b class="font-monospace"><%= employee.phone_number_one %></b></p>
    <p class="lead">Telefone: <b class="font-monospace"><%= employee.phone_number_two %></b></p>
    <p class="lead">Salário: <b class="font-monospace"><%= employee.salary %></b></p>
    <p class="lead">INSS: <b class="font-monospace"><%= employee.inss %></b></p>
    <p class="lead">Lagradouro: <b class="font-monospace"><%= employee.street %></b></p>
    <p class="lead">Número: <b class="font-monospace"><%= employee.number %></b></p>
    <p class="lead">Bairro: <b class="font-monospace"><%= employee.neighborhood %></b></p>
    <p class="lead">Cidade: <b class="font-monospace"><%= employee.city %></b></p>
    <p class="lead">Estado: <b class="font-monospace"><%= employee.state %></b></p>
    <p class="lead">CEP: <b class="font-monospace"><%= employee.cep %></b></p>
    <p class="lead">Data de cadastro: <%= employee.created_at.strftime('%d/%m/%Y') %></p>
    <p class="lead">Ultima atualização: <%= employee.updated_at.strftime('%d/%m/%Y') %></p>

    <div class="d-grid gap-2 mx-auto">
        <%= link_to "Editar funcionário", edit_employee_path(employee), data: { turbo_method: :get }, class: "btn btn-secondary" %>
        <% if current_user.try(:admin?) %>
            <%= link_to "Excluir funcionário?", @employee, class: "btn btn-danger",  data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
        <% end %>
        <%= link_to "Retorne para funcionários", employees_path, class: "link-offset-2" %>
    </div>

</div>